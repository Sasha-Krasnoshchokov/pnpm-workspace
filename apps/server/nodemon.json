{
  "watch": ["src"],
  "ext": "ts,json",
  "ignore": ["src/**/*.test.ts", "dist"],
  "exec": "tsx watch src/server.ts",
  "args": "src/server.ts",
  "delay": 500,
  "env": {
    "DOTENV_CONFIG_PATH": "./.env.local"
  }
}
